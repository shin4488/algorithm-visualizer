{
  "name": "algorithm-visualizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html --dist-dir public",
    "build": "parcel build src/index.html --dist-dir public",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "bash -c 'rm -rf .eslint-tmp && tsc -p tsconfig.lint.json && yarn dlx eslint .eslint-tmp --ext .js \"$@\"; status=$?; rm -rf .eslint-tmp; exit $status' --",
    "format": "yarn dlx prettier --check \"src/**/*.{ts,tsx,css}\" package.json tsconfig.json tsconfig.lint.json eslint.config.js .prettierrc.json \".github/**/*.yml\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/react": "^14.2.1",
    "@testing-library/user-event": "^14.5.2",
    "@types/react": "^18.2.24",
    "@types/react-dom": "^18.2.9",
    "jsdom": "^24.0.0",
    "parcel": "^2.12.0",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
